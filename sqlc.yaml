version: "2"
sql:
  - engine: "sqlite"
    queries: "./internal/adapters/repositories/sqlite/queries/*.sql"
    schema: schema.sql
    gen:
      go:
        package: "sqlc"
        out: "./internal/adapters/repositories/sqlite/generated"